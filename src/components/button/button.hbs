export default `
<button
    {{#if path}}data-path='{{path}}'{{/if}}
    type='{{#if type}}{{type}}{{else}}button{{/if}}'
    {{#if (eq theme "default")}}class='btn {{styles.btn}} {{className}}'
    {{else}}class='btn {{className}}'{{/if}}
    name='{{name}}'>
  {{{text}}}
</button>`
;
