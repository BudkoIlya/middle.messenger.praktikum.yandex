export default `
<button
    {{#if path}}data-path='{{path}}'{{/if}}
    {{#if id}}data-id='{{id}}'{{/if}}
    type='{{#if type}}{{type}}{{else}}button{{/if}}'
    class='btn {{styles.btn}} {{className}}' name='{{name}}'>
  {{{text}}}
</button>`
;
