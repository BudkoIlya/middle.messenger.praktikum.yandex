export default `<label class="label">
  {{#if title}}<span class="title">{{title}}</span>{{/if}}
  <input class="input {{class}}" placeholder="{{placholder}}" title="{{title}}" name="{{name}}">
</label>`;